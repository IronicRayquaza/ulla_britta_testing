name: Intentional Failure Test

on: [push]

jobs:
  fail-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Force failure
        run: |
          echo "Forcing failure..."
          exit 1
#testing shit 
